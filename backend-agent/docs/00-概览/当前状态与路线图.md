# 当前状态与路线图

> 定位：活文档。每次合入更新，让任何人（含 AI）一眼知道"现在站在哪、下一步做什么"。
> 维护规则：只记事实（已完成 / 进行中 / 下一步），不在这里写设计细节；细节去对应模块。

## 阶段总览

| 阶段 | 主题 | 状态 |
| --- | --- | --- |
| 一期 | 最简可用 Agent Runtime（单 Agent 闭环） | ✅ 已落地（代码 + 测试） |
| 二期 | 会话、治理与可恢复运行 | 🚧 设计完成，代码待实现 |
| 三期 | 编排与企业能力 | ⏳ 未开始（仅方向） |

## 一期（已完成）

- `POST /v1/agents/{agent_id}/test`、`/stream`、`POST /v1/model/test`、`GET /v1/agents`、`GET /v1/traces/{trace_id}`。
- 代码：`app/core/runtime.py`、`app/infrastructure/model_gateway.py`、`app/infrastructure/recorder.py`、`app/tools/*`；测试：`tests/`。

## 二期（设计完成，代码待实现）

按 [阶段二范围与验收方向.md](阶段二范围与验收方向.md) 的四条主线：

| 主线 | 设计方案 | 代码 |
| --- | --- | --- |
| 1. 对话持久化与上下文选择（最高优先级） | [../20-模块/session/总文档.md](../20-模块/session/总文档.md) | ❌ 未开始 |
| 2. 最小治理与权限边界 | 待补模块文档（当前仅有方向） | ❌ 未开始 |
| 3. 插件合同与沙箱基础 | [../20-模块/sandbox/总文档.md](../20-模块/sandbox/总文档.md) | ❌ 未开始 |
| 4. 服务交互、数据边界与共享追踪 | 待补模块文档（当前仅有方向） | ❌ 未开始 |

## 下一步

当前优先待办（可勾选、按优先级）见 [TODO.md](TODO.md)。

## 术语提醒

持久化模块的"Phase 1"与项目"一期"指代不同层级：前者是"持久化模块"的第一阶段，后者是"项目"的一期（JSONL trace）。存储选型已决策：先 SQLite、后迁移 PostgreSQL，见 [决策记录](../10-决策记录/10-决策记录.md) D-002。
